---
_id: "69060294f7929b4140541fea"
title: "Doc: NGINX + SSL by Letsencrypt (part 1.3) // Ultimate guide 2025"
brief: 'DX'
bg_src: "/static/img/blog/FB_IMG_1787608253773.webp"
bg_size: { w: 2048, h: 2048 }
bg_type: "image/webp"

createdAt: "2025-11-01T12:52:36.824Z"
updatedAt: "2026-07-07T20:36:42.265Z"
priority: 0

author: "Den Pol"
tags: ["this_project", "doc", "certbot", "ssl", "pravosleva.pro"]
#category: "guides"
isPrivate: true
---
> Это статья про внутреннее развитие этого сайта в рамках которого затрагиваются и более общие вопросы по разработке

# Step 1. Create webroot dir

```shell
mkdir /var/www/letsencrypt
```

Then go on to Step 2...

# Step 2. NGINX
```shell
cd /ect/nginx/conf.d
```

`50-pravosleva.pro.conf`
```nginx
server {
  listen 80;
  listen 443 ssl;

  server_name .pravosleva.pro;
  
  ssl_certificate /etc/letsencrypt/live/pravosleva.pro/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/pravosleva.pro/privkey.pem;
  ssl_protocols        TLSv1.2 TLSv1.1 TLSv1;
  ssl_prefer_server_ciphers on;
  ssl_ciphers 'kEECDH+ECDSA+AES128 kEECDH+ECDSA+AES256 kEECDH+AES128 kEECDH+AES256 kEDH+AES128 kEDH+AES256 DES-CBC3-SHA +SHA !aNULL !eNULL !LOW !MD5 !EXP !DSS !PSK !SRP !kECDH !CAMELLIA !RC4 !SEED';
  ssl_session_cache   shared:SSL:10m;
  ssl_session_timeout 10m;

  location ^~ /.well-known/acme-challenge/ {
    default_type "text/plain";
    root /var/www/letsencrypt;
  }
  location = /.well-known/acme-challenge/ {
    return 404;
  }

  # other static builds
  location /dist.family-tree-2023 {
    index index.html;
    root /root/projects;
    #try_files $uri $uri/ $uri.html =404;
    autoindex on;
  }
  location /dist.sp-tradein-2023 {
    index index.html;
    root /root/projects;
    autoindex on;
  }
  location /dist.cargo-2016 {
    index index.html;
    root /root/projects;
    autoindex on;
  }
  location /dist.cargo-3d {
    index index.html;
    root /root/projects;
    autoindex on;
  }
  location /dist.sp-tradein-2024 {
    auth_basic "Secret space";
    auth_basic_user_file /var/www/html/.htpasswd;
    index index.html;
    root /root/projects;
    autoindex on;
  }
  location /dist.hacker-news-2024 {
    index index.html;
    root /root/projects;
    autoindex on;
  }
  location /dist.estimate-corrector-2024 {
    index index.html;
    root /root/projects;
    autoindex on;
  }
  location /d/sounds-in-access-2025 {
    index index.html;
    root /root/projects;
    autoindex on;
  }

  # nextjs
  location /static {
    root /root/projects/pravosleva-blog/frontend.nextjs/public;
  }
  location ~ ^/_next/static/(.*)$ {
    add_header Cache-Control "max-age=31536000";
    root /root/projects/pravosleva-blog/frontend.nextjs/.next;
    try_files "/static/$1" "/server/static/$1" @proxy_pass;

    # kill cache
    add_header Last-Modified $date_gmt;
    add_header Cache-Control 'no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0';
    if_modified_since off;
    # expires off;
    expires -1;
    etag off;
  }
  location /socket.io {
    proxy_set_header  X-Real-IP  $remote_addr;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header Test "hello world from nginx";
    proxy_pass http://nextjs;
  }
  location / {
    proxy_set_header   X-Real-IP $remote_addr;
    if ($request_method = 'OPTIONS') {
      add_header 'Access-Control-Allow-Origin' '*';
      add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
      #
      # Custom headers and headers various browsers *should* be OK with but aren't
      #
      add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
      #
      # Tell client that this pre-flight info is valid for 20 days
      #
      add_header 'Access-Control-Max-Age' 1728000;
      add_header 'Content-Type' 'text/plain; charset=utf-8';
      add_header 'Content-Length' 0;
      return 204;
    }
    if ($request_method = 'GET') {
      add_header 'Access-Control-Allow-Origin' '*' always;
      add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
      add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;
      add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;
    }
    proxy_pass http://nextjs;
  }

  # helper
  location ~ ^/express-helper/auth/signin/(.*)$ {
    proxy_buffering off;
    proxy_redirect  off;
    proxy_set_header  Host  $http_host;
    proxy_set_header  X-Forwarder-For  $remote_addr;
    proxy_set_header  X-Frame-Options  SAMEORIGIN;
    proxy_pass   http://helper/auth/signin/$1;
  }
  location ~ ^/express-helper/chat/(.*)$ {
    proxy_buffering off;
    proxy_redirect  off;
    proxy_set_header  Host  $http_host;
    proxy_set_header  X-Forwarder-For  $remote_addr;
    proxy_set_header  X-Frame-Options  SAMEORIGIN;
    proxy_pass   http://helper/chat/$1;
  }
  location /express-helper/ {
    proxy_cache_bypass 1;
    max_ranges 0;
    #add_header Origin-Agent-Cluster "?1";
    proxy_pass http://helper/;
  }

  # tg-bot
  location /tg-bot-2021/ {
    proxy_pass http://tg-bot/;
  }

  # strapi@4.x
  location /strapi/ {
    proxy_pass http://strapi/;
  }
  location ~ ^/(admin|i18n|api|content-manager|upload|content-type-builder|documentation|strapi-google-auth|plugins|graphql)/ {
    proxy_pass http://strapi;
  }
}
```

`51-gosuslugi.pravosleva.pro.conf`
```nginx
server {
  listen 80;
  #listen 443 ssl;
  listen 443;
  server_name gosuslugi.pravosleva.pro;

  #location ~ /.well-known {
  #  allow all;
  #  root /home/projects/---/cert-output/;
  #}
  location ^~ /.well-known/acme-challenge/ {
    default_type "text/plain";
    root /var/www/letsencrypt;
  }

  location / {
    proxy_set_header Host pravosleva.pro;
    proxy_pass http://127.0.0.1:80;
  }
  location /socket.io {
    proxy_pass http://helper;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host pravosleva.pro;
  }
}
```

`52-www.pravosleva.pro.conf`
```nginx
server {
  listen 80;
  listen 443 ssl;
  server_name www.pravosleva.pro;

  location /socket.io {
    proxy_pass http://helper;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host pravosleva.pro;
  }

  location ^~ /.well-known/acme-challenge/ {
    default_type "text/plain";
    root /var/www/letsencrypt;
  }

  location / {
    proxy_set_header Host pravosleva.pro;
    proxy_pass http://127.0.0.1:80;
  }
}
```

# Step 3. Install certbot
(skipped)

# Step 4. Fresh SSL generation

Before:
```shell
certbot certonly --dry-run -d pravosleva.pro -d www.pravosleva.pro -d gosuslugi.pravosleva.pro
```

In progress:
```
How would you like to authenticate with the ACME CA?
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
1: Spin up a temporary webserver (standalone)
2: Place files in webroot directory (webroot)
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Select the appropriate number [1-2] then [enter] (press 'c' to cancel): 2
```

In progress:
```
You have an existing certificate that contains a portion of the domains you
requested (ref: /etc/letsencrypt/renewal/pravosleva.pro.conf)

It contains these names: pravosleva.pro, www.pravosleva.pro

You requested these names for the new certificate: pravosleva.pro,
www.pravosleva.pro, gosuslugi.pravosleva.pro.

Do you want to expand and replace this existing certificate with the new
certificate?
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
(E)xpand/(C)ancel: E
```

Input webroot:
```
Renewing an existing certificate
Performing the following challenges:
http-01 challenge for gosuslugi.pravosleva.pro
http-01 challenge for pravosleva.pro
http-01 challenge for www.pravosleva.pro
Input the webroot for gosuslugi.pravosleva.pro: (Enter 'c' to cancel): /var/www/letsencrypt
```

Then:
```
Select the webroot for pravosleva.pro:
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
1: Enter a new webroot
2: /var/www/letsencrypt
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Select the appropriate number [1-2] then [enter] (press 'c' to cancel): 2
```

And again:
```
Select the webroot for www.pravosleva.pro:
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
1: Enter a new webroot
2: /var/www/letsencrypt
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Select the appropriate number [1-2] then [enter] (press 'c' to cancel): 2
```

Finally:
```
Waiting for verification...
Cleaning up challenges

IMPORTANT NOTES:
 - The dry run was successful.
```

So, we ready to go on:
```
certbot certonly -d pravosleva.pro -d www.pravosleva.pro -d gosuslugi.pravosleva.pro
Saving debug log to /var/log/letsencrypt/letsencrypt.log

How would you like to authenticate with the ACME CA?
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
1: Spin up a temporary webserver (standalone)
2: Place files in webroot directory (webroot)
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Select the appropriate number [1-2] then [enter] (press 'c' to cancel): 2
Plugins selected: Authenticator webroot, Installer None

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
You have an existing certificate that contains a portion of the domains you
requested (ref: /etc/letsencrypt/renewal/pravosleva.pro.conf)

It contains these names: pravosleva.pro, www.pravosleva.pro

You requested these names for the new certificate: pravosleva.pro,
www.pravosleva.pro, gosuslugi.pravosleva.pro.

Do you want to expand and replace this existing certificate with the new
certificate?
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
(E)xpand/(C)ancel: E
Renewing an existing certificate
Performing the following challenges:
http-01 challenge for gosuslugi.pravosleva.pro
Input the webroot for gosuslugi.pravosleva.pro: (Enter 'c' to cancel): /var/www/letsencrypt
Waiting for verification...
Cleaning up challenges

IMPORTANT NOTES:
 - Congratulations! Your certificate and chain have been saved at:
   /etc/letsencrypt/live/pravosleva.pro/fullchain.pem
   Your key file has been saved at:
   /etc/letsencrypt/live/pravosleva.pro/privkey.pem
   Your cert will expire on 2026-10-05. To obtain a new or tweaked
   version of this certificate in the future, simply run certbot
   again. To non-interactively renew *all* of your certificates, run
   "certbot renew"
 - If you like Certbot, please consider supporting our work by:

   Donating to ISRG / Let's Encrypt:   https://letsencrypt.org/donate
   Donating to EFF:                    https://eff.org/donate-le
```

And restart NGINX:
```bash
systemctl restart nginx
```

<ControlsBox
  controlsJson='[
    {
      "title": "PREV",
      "label": "pravosleva.pro deployer // Создание пользователя Ubuntu",
      "type": "link",
      "link": "/p/_no-index.this-project-doc-1-2-nginx"
    }
  ]'
/>
