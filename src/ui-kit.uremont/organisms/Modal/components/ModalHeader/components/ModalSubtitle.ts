import styled from 'styled-components'
import { themeTextStyle, themeColor } from '~/ui-kit.uremont'

export const ModalSubtitle = styled('p')`
  ${themeTextStyle('12R Notifications')}
  color: ${themeColor('Gray-blue')};
`
